package test;

import core.ClusterListener;
import routing.BlossomUtilities.Cluster;

public class ClusterChecker implements ClusterListener {
    @Override
    public void newCluster(Cluster c) {

    }

    @Override
    public void removeCluster(Cluster c) {

    }
}
